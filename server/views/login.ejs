<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Login</title>
  <link rel="stylesheet" href="/static/styles.css"/>
  <% if (brandPrimary) { %><style>:root{ --primary:<%= brandPrimary %>; }</style><% } %>
</head>
<body>
  <div class="login-wrap" style="background: var(--bg);">
    <div class="card login-card" style="max-width: 420px;">
      <h2 class="login-title">Login</h2>
      <% if (error) { %><div class="panel" style="color:#b91c1c; margin-bottom:10px"><%= error %></div><% } %>
      <form method="post" action="/login">
        <div style="display:flex; flex-direction:column; gap:10px;">
          <input type="password" name="password" placeholder="Password" required style="padding:12px 14px; border:1px solid #d1d5db; border-radius:10px;"/>
          <div style="display:flex; gap:10px; justify-content:flex-start;">
            <button type="reset" class="btn btn-muted">Clear</button>
            <button type="submit" class="btn btn-primary">Login</button>
          </div>
        </div>
      </form>
      <div class="footer"><b><%= footerText %></b></div>
    </div>
  </div>
</body>
</html>
